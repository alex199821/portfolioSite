import Wrapper from "../assets/wrappers/UnderlinedString";
const UnderlinedString = ({ string }) => {
  return (
    <Wrapper>
      <p>{string}</p>
    </Wrapper>
  );
};

export default UnderlinedString;
